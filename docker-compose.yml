services:
  agentCommand:
    image: node:alpine
    container_name: agentCommand
    network_mode: "host"
    volumes:
      - .:/app
    working_dir: /app
    environment:
      - NODE_ENV=development
    command: ["npm", "run", "start:commandcenter"]
